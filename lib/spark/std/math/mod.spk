~forge~ = calm

@spells@
pub fn add(a: i32, b: i32) -> i32 {
    a + b
}

~forge~ = balanced
pub fn multiply(a: i32, b: i32) -> i32 {
    a * b
}

~forge~ = wild
pub unsafe fn raw_ptr_add(a: *mut i32, b: i32) -> i32 {
    unsafe { *a + b }
}
@spells@
